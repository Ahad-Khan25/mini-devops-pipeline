apiVersion: v1
kind: Service
metadata:
  name: mini-devops-app-service
spec:
  type: NodePort
  selector:
    app: mini-devops-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
